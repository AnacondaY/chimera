### 禁用
使输入框不可用。
```javascript
    class Demo extends React.Component{
        render(){
            return (
                <NumberInput className="demo-number-input" disabled />
            )
        }
    }
```